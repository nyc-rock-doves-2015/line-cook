post '/favorites' do
  # could create a parser class to do the below JSON-parsing actions
  # big_oven_id = parsed json request for big_oven_id number
  # user_name = parsed json request for user_name
  # recipe = find or create a recipe by searching DB for big_oven_id
  # user = find user by user_name variable
  # favorite = create new favorite with recipe and user data
  # redirect to user's dashboard with new favorite object
end
